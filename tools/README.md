# Interactive tools

Each tool runs the real VesselDSP engine in the browser. No offline plots.

The bar: a visitor with a guitar and no account can hear a modeling tradeoff change within 10 seconds of landing.

Implementation lives in the web app, not here. This directory holds each tool's spec: what it demonstrates, what the user manipulates, what they see and hear, and which lesson it belongs to.
